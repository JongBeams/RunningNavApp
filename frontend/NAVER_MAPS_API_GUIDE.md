# 네이버 Maps API 호환성 가이드

## 현재 상태

- **사용 중**: 구 AI NAVER API (종료 예정)
- **클라이언트 ID**: `gb25z9esgh`
- **문제**: WebView에서 `localhost` 도메인 인증 실패

## 옵션 비교

### 옵션 1: 구 API 계속 사용 (임시)

#### 장점
- 기존 클라이언트 ID 사용 가능
- 코드 변경 최소화

#### 단점
- ⚠️ **서비스 종료 예정**
- 웹 서비스 URL 등록 필요

#### 설정 방법
1. [네이버 개발자 센터](https://developers.naver.com/apps/) 접속
2. 애플리케이션 선택 (클라이언트 ID: `gb25z9esgh`)
3. **웹 서비스 URL** 설정에 다음 추가:
   - `https://localhost`
   - `http://localhost`
4. 저장 후 앱 재시작

---

### 옵션 2: 신규 네이버 클라우드 플랫폼 API (권장)

#### 장점
- ✅ **장기 지원 보장**
- ✅ **도메인 제한 더 유연**
- ✅ **최신 기능 지원**

#### 단점
- 새 클라이언트 ID 발급 필요
- 코드 약간 수정 필요

#### 설정 방법

##### 1단계: 클라이언트 ID 발급
1. [네이버 클라우드 플랫폼 콘솔](https://console.ncloud.com/) 접속
2. Services > Application Service > **Maps** 선택
3. **AI·NAVER API** 클릭
4. **Application 등록** 클릭
5. 애플리케이션 정보 입력:
   - 애플리케이션 이름: `RunRun` (또는 원하는 이름)
   - Service: **Maps** 선택
   - 서비스 환경: **Web Dynamic Map** 선택
6. **Web 서비스 URL** 등록:
   - `https://localhost`
   - `http://localhost`
7. **등록** 클릭하여 클라이언트 ID 받기

##### 2단계: 코드 수정
`frontend/src/components/map/naverMapHtml.ts` 파일에서:

```typescript
// 발급받은 클라이언트 ID로 교체
const NAVER_CLIENT_ID_NEW = 'YOUR_NEW_CLIENT_ID'  // ← 여기에 발급받은 ID 입력

// 신규 API 사용으로 변경
const USE_NEW_API = true;  // false → true로 변경
```

##### 3단계: 테스트
앱을 재시작하고 지도가 정상적으로 로드되는지 확인

---

## WebView 호환성 검증 결과

### ✅ 호환 가능
- 네이버 Maps JavaScript API v3는 WebView에서 정상 작동
- React Native WebView와 호환됨
- Android/iOS 모두 지원

### 주의사항
1. **인증 필수**: 클라이언트 ID와 도메인 등록 필요
2. **인터넷 연결**: 지도 타일 로드를 위해 필요
3. **위치 권한**: 현재 위치 표시 기능 사용 시 필요

---

## 추천 방안

### 🎯 단기 (지금 당장 테스트용)
**옵션 1 사용**: 네이버 개발자 센터에서 `localhost` 등록
- 빠른 테스트 가능
- 5분 내 적용 가능

### 🎯 장기 (프로덕션 배포용)
**옵션 2로 전환**: 신규 네이버 클라우드 플랫폼 API 사용
- 서비스 안정성 보장
- 향후 업데이트 지원

---

## 파일 구조

```
frontend/src/components/map/
├── NaverMapWebView.tsx       # WebView 컴포넌트
├── naverMapHtml.ts           # 지도 HTML (API 설정)
└── NAVER_MAPS_API_GUIDE.md  # 이 문서
```

---

## 문제 해결

### 지도가 표시되지 않는 경우

1. **인증 확인**
   - 로그에서 "Authentication Failed" 확인
   - 웹 서비스 URL이 올바르게 등록되었는지 확인

2. **네트워크 확인**
   - 인터넷 연결 상태 확인
   - 방화벽 설정 확인

3. **디버그 로그 확인**
   ```bash
   adb logcat -d | grep -i "chromium\|console"
   ```

---

## 참고 링크

- [네이버 개발자 센터](https://developers.naver.com/)
- [네이버 클라우드 플랫폼](https://www.ncloud.com/)
- [신규 API 가이드](https://navermaps.github.io/maps.js.ncp/docs/tutorial-2-Getting-Started.html)
- [공지사항](https://www.ncloud.com/support/notice/all/1930)
